var TreeNode = cc.Node.extend({
    ctor: function () {
        this.super();
    },
    generate: function (data) {
        var length = data.length;
        // var lx
        Game.chance.integer({min: -20, max: 20})
    }
});